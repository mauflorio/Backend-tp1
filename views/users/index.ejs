<%- include('../partials/header') %>

<h2>Usuarios</h2>
<a href="/users/new">Crear nuevo usuario</a>
<ul>
  <% users.forEach(u => { %>
    <li>
      <a href="/users/<%= u.id %>"><%= u.nombre %></a> - <%= u.rol || 'Sin rol' %>
    </li>
  <% }) %>
</ul>

<%- include('../partials/footer') %>
